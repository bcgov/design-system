import { Meta } from "@storybook/blocks";

<Meta title="Introduction" />

# BC Design System React Components

This Storybook instance holds React component stories for the BC Design System, as used by the library `@bcgov/design-system-react-components`.

Open the "Components" menu on the left to explore the components that are currently available.
